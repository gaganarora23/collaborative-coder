services:
  - type: web
    name: collaborative-coder
    env: docker
    plan: free
    region: oregon 
    envVars:
      - key: NODE_ENV
        value: production
    dockerContext: .
    dockerfilePath: Dockerfile
